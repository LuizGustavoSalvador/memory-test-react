version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3333:3333'
    depends_on:
      - mongo
    volumes:
      - ./backend:/usr/app/backend
      - ./frontend:/usr/app/frontend

  mongo:
    image: mongo
    ports:
      - '27017:27017'
